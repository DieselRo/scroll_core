---
title: Open Threads
status: active
audience: user, operator
---

# Open Threads

Open threads make unresolved work visible and trackable across scrolls. Use them to capture validation issues, TODOs, or follow-ups generated by services.

## When to open a thread

- Validation finds a broken link or missing header in a scroll.
- A service flags an action item (e.g., index rebuild required).
- Operators need to track a fix across multiple commits.

## Examples

- Create: `cargo run -- open-threads --action create --title "Fix missing header" --scroll scrolls/001_intro.md --assignee aelren`
- List open for a path: `cargo run -- open-threads --action list --status OPEN --scroll scrolls/001_intro.md`
- Close: `cargo run -- open-threads --action close <id> --reason "Header added"`

